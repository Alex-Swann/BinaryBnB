block content
  h2 #{title}
  - if(user)
    form(action='/signOut', method='post')
      input(type='submit', value='Sign Out' id='SignOut')
    #welcomeUser
      | Welcome #{user.name}

    a.button(href="spaces/new", class='space') List a space


  ul#spaces
    - for (var i = 0; i < spaces.length; i++)
      li
        a.button(href="requests/new", class='request') #{spaces[i].name}
        ul
          li
            | Price: £
            span.price #{spaces[i].price}
          li
            | Description:
            span.description #{spaces[i].description}
          li
            | Availability:
            span.availablefrom #{spaces[i].availablefrom}
            | -
            span.availableto #{spaces[i].availableto}

function find(arr, field, val) {
  var out;
  arr.some(function(item) {
    if (item[field] === val) {
      out = item;
      return true;
    }
  });
  return out;
}

exports = module.exports = find;